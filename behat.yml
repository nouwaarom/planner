default:
  extensions:
    Behat\Symfony2Extension: ~
    Behat\MinkExtension:
      base_url: http://planner.dev
      sessions:
        default:
          goutte: ~

  suites:
    default:
      contexts:
        - FeatureContext:
            #session: '@session'
